name: Publish Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: universal-apple-darwin
            config: '{"bundle":{"targets":["app","dmg"]}}'
            name: macOS

          - platform: ubuntu-22.04
            target: ""
            config: '{"bundle":{"targets":["deb","appimage"]}}'
            name: Linux

          - platform: windows-latest
            target: ""
            # ✅ 关键修复：把 "zh-CN" 改为 "SimpChinese"
            config: '{"bundle":{"targets":["nsis"],"windows":{"nsis":{"installMode":"perMachine","languages":["SimpChinese"],"displayLanguageSelector":false}}}}'
            name: Windows

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Clean Cargo proxy config
        shell: bash
        run: rm -rf ~/.cargo/config* .cargo/config* || true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'
          cache-on-failure: true

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev \
            librsvg2-dev patchelf build-essential curl wget file libssl-dev libxdo-dev

      - name: Install frontend dependencies
        run: npm ci

      # 生成动态配置（Windows 已使用 SimpChinese）
      - name: Generate platform-specific Tauri config
        run: echo '${{ matrix.config }}' > tauri-build-config.json

      # 构建 + 发布 Release
      - name: Build Tauri app & create GitHub Release
        uses: tauri-apps/tauri-action@v0.6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "SafeMask ${{ github.ref_name }}"
          releaseBody: |
            🚀 **SafeMask ${{ github.ref_name }}** 已发布！

            **下载地址** 请见下方 Assets。
          releaseDraft: true
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') || contains(github.ref_name, 'alpha') }}
          args: --config tauri-build-config.json ${{ matrix.target != '' && format('--target {0}', matrix.target) || '' }}

      - name: Create Windows Portable ZIP
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          $portableDir = "SafeMask_${version}_Portable_x64"
          $zipPath = "${portableDir}.zip"

          # 从构建日志确认的真实路径（相对路径）
          $nsisSetupExe = "src-tauri/target/release/bundle/nsis/SafeMask_${version}_x64-setup.exe"

          Write-Output "预期 NSIS exe 路径: $nsisSetupExe"

          if (-not (Test-Path $nsisSetupExe)) {
            Write-Output "路径不存在！列出 bundle/nsis 目录内容（如果存在）："
            if (Test-Path "src-tauri/target/release/bundle/nsis") {
              Get-ChildItem -Path "src-tauri/target/release/bundle/nsis" -Force | ForEach-Object { Write-Output $_.FullName }
            } else {
              Write-Output "bundle/nsis 目录完全不存在！"
            }

            Write-Output "递归列出 target/release 下所有 .exe（调试用）："
            Get-ChildItem -Path "src-tauri/target/release" -Recurse -Filter "*.exe" -ErrorAction SilentlyContinue | ForEach-Object { Write-Output $_.FullName }

            throw "NSIS exe 文件未找到：$nsisSetupExe。请检查构建日志确认文件名/路径。"
          }

          Write-Output "找到 NSIS exe: $nsisSetupExe （将用作 portable 版）"

          # 创建便携目录并拷贝，重命名为 SafeMask.exe（去掉 -setup 后缀，用户更友好）
          New-Item -ItemType Directory -Path $portableDir -Force | Out-Null
          Copy-Item -Path $nsisSetupExe -Destination "$portableDir/SafeMask.exe" -Force

          # 拷贝 resources（如果存在）
          $resDir = "src-tauri/target/release/resources"
          if (Test-Path $resDir) {
            Copy-Item -Recurse -Force $resDir "$portableDir/resources"
            Write-Output "已拷贝 resources"
          }

          Compress-Archive -Path "$portableDir\*" -DestinationPath $zipPath -Force
          Write-Output "便携 ZIP 生成完成: $zipPath"

          gh release upload ${{ github.ref_name }} $zipPath --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}