name: "Publish Release"

on:
  push:
    tags:
      - 'v*' # 当推送到以 v 开头的标签时触发，如 v1.1.0.0

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest" # macOS (Intel & Apple Silicon)
            args: "--target universal-apple-darwin"
          - platform: "ubuntu-22.04" # Linux
            args: ""
          - platform: "windows-latest" # Windows
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      
       # 🚀 新增步骤：移除本地代理配置，防止 CI 环境连接失败
      - name: Remove local cargo proxy
        run: |
          if [ -d ".cargo" ]; then
            rm -rf .cargo
            echo "Removed .cargo directory to prevent proxy errors in CI"
          fi
        shell: bash

      # 1. 环境准备：Node.js
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # 2. 环境准备：Rust
      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          # 针对 macOS 需要添加 aarch64 目标以支持 Universal 二进制
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      # 3. Rust 编译缓存（大幅提升二次打包速度）
      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      # 4. 安装 Linux 必要的系统依赖 (Tauri v2 专用)
      - if: matrix.platform == 'ubuntu-22.04'
        name: install dependencies (ubuntu only)
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

      # 5. 安装前端依赖
      - name: install frontend dependencies
        run: npm install

      # 6. 构建并发布
      - name: build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }} # 使用当前推送到标签名
          releaseName: "SafeMask v__VERSION__" # 版本号会自动从 tauri.conf.json 提取
          releaseBody: "🚀 SafeMask 极致性能脱敏工具新版本发布。详情请见下方附件。"
          releaseDraft: true # 先作为草稿发布，确认无误后再手动点击公开
          prerelease: false
          args: ${{ matrix.args }}