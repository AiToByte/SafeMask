name: Publish Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: universal-apple-darwin
            config: '{"bundle":{"targets":["app","dmg"]}}'
            name: macOS

          - platform: ubuntu-22.04
            target: ""
            config: '{"bundle":{"targets":["deb","appimage"]}}'
            name: Linux

          - platform: windows-latest
            target: "x86_64-pc-windows-msvc"
             # ✅ 深度优化：启用官方 portable 模式，生成单文件免安装版，彻底解决资源路径闪退问题
            config: '{"bundle":{"targets":["nsis"],"windows":{"nsis":{"type":"portable","installMode":"perMachine","languages":["SimpChinese"],"displayLanguageSelector":false}}}}'
            name: Windows

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Clean Cargo proxy config
        shell: bash
        run: rm -rf ~/.cargo/config* .cargo/config* || true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          # 仅在需要时安装特定 target
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || (matrix.target != '' && matrix.target || '') }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'
          cache-on-failure: true

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev \
            librsvg2-dev patchelf build-essential curl wget file libssl-dev libxdo-dev

      - name: Install frontend dependencies
        run: npm ci

      # 生成动态配置（Windows 已使用 SimpChinese）
      - name: Generate platform-specific Tauri config
        run: echo '${{ matrix.config }}' > tauri-build-config.json

      # 构建主体（包含安装包和便携版）
      - name: Build Tauri app & create GitHub Release
        uses: tauri-apps/tauri-action@v0.6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "SafeMask ${{ github.ref_name }}"
          releaseBody: |
            🚀 **SafeMask ${{ github.ref_name }}** 现已正式发布！
            
            SafeMask 是一款基于 Rust 构建的高性能本地隐私脱敏工具，确保您的敏感数据在拥抱 AI 的同时，隐私不出域。

            ### 📦 各平台下载指南 / Download Guide

            #### 🪟 Windows
            - **标准安装版 (`SafeMask_x64-setup.exe`)**: 建议大多数用户使用，支持快捷方式和自动更新。
            - **单文件便携版 (`SafeMask_Portable_x64.exe`)**: 无需安装，双击即用，适合 U 盘或快速使用。

            #### 🍎 macOS
            - **磁盘映像 (`.dmg`)**: 通用二进制版本，支持 Apple Silicon (M1/M2/M3) 和 Intel 芯片。

            #### 🐧 Linux
            - **通用版 (`.AppImage`)**: 赋予权限即可运行。
            - **安装包 (`.deb`)**: 适用于 Ubuntu/Debian。

            ---
            ### 🔐 安全承诺 / Security
            - **100% Local**: 所有脱敏计算均在本地完成。引擎无任何网络权限，数据绝对安全。

            ---
            *感谢使用 SafeMask！*
          releaseDraft: true
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') || contains(github.ref_name, 'alpha') }}
          args: --config tauri-build-config.json ${{ matrix.target != '' && format('--target {0}', matrix.target) || '' }}
          
      # 专门处理 Windows 的单文件便携版上传
      - name: Handle Windows Portable Artifact
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          $target = "${{ matrix.target }}"
          $version = "${{ github.ref_name }}".TrimStart('v')
          
          # 1. 定位 NSIS 产物目录
          $basePath = if ($target) { "target\$target\release\bundle\nsis" } else { "target\release\bundle\nsis" }
          
          Write-Host "正在搜索便携版产物，路径: $basePath"
          
          if (-not (Test-Path $basePath)) {
              Write-Error "找不到产物目录！打印目录树以供排查："
              Get-ChildItem -Path "target" -Recurse -Depth 3
              exit 1
          }

          # 2. 寻找生成的 portable 文件 (由 "type": "portable" 生成)
          $portableExe = Get-ChildItem -Path $basePath -Filter "SafeMask_*_x64-portable.exe" | Select-Object -First 1
          
          if ($portableExe) {
              Write-Host "找到官方便携版: $($portableExe.FullName)"
              # 重命名为更友好的名称
              $newName = "SafeMask_${version}_Portable_x64.exe"
              Move-Item -Path $portableExe.FullName -Destination $newName -Force
              
              # 上传到 Release
              gh release upload ${{ github.ref_name }} "${newName}#Windows-Portable-x64" --clobber
              Write-Host "上传成功！"
          } else {
              Write-Error "在 $basePath 中找不到 portable.exe。请确认 matrix 中的 config 已包含 type:portable"
              exit 1
          }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}